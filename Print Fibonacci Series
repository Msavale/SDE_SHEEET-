vector<int> generateFibonacciNumbers(int n) {
    // Write your code here.

    vector <int> fib(n , 0);

    fib [0] = 0;
    fib [1] = 1;
    fib [2] = 1;

    for (int i = 3 ; i < n ; i++){

        fib[i] = fib[i-1] + fib[i-2];
       
    }

    return fib;
}
