bool check(int i,string& str)

{

  if(i>=str.size()/2)

  return true;

  if(str[i]==str[str.size()-1-i])

        check(i+1,str);

  else

  return false;

}

bool isPalindrome(string& str) {

                return   check(0,str);

}
