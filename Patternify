#include <bits/stdc++.h> 
vector<string> printPatt(int n)
{
    vector<string> result;

    for(int i = 0; i < n; i++){

        string line(n - i, '*');

        result.push_back(line);

    }

    return result;
}
