vector<int> rotateArray(vector<int> arr, int k)

 {

     int size=arr.size();

     for(int i=0;i<k;i++)

     {   

         int temp=arr[0];

         for(int j=0;j<size-1;j++)

         {

          arr[j] = arr[j+1];

         }

         arr[size-1]=temp;

     }

     return arr;

}
